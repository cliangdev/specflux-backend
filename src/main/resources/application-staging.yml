# Staging Profile Configuration

spring:
  # Database (Supabase PostgreSQL via transaction pooler)
  datasource:
    url: ${DATABASE_URL}
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      connection-timeout: 20000
      # Required for Supabase pooler (PgBouncer) - sets search_path on each connection
      connection-init-sql: SET search_path TO public

  jpa:
    properties:
      hibernate:
        default_schema: public

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
    default-schema: public

server:
  port: 8090

# Actuator - health probes for Cloud Run
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      probes:
        enabled: true
      show-details: always

# Logging - staging levels
logging:
  level:
    com.specflux: INFO
    org.springframework: WARN

# Springdoc - enable in staging for testing
springdoc:
  swagger-ui:
    enabled: true
  api-docs:
    enabled: true

# GitHub App Integration
github:
  app:
    id: ${GITHUB_APP_ID}
    client-id: ${GITHUB_CLIENT_ID}
    client-secret: ${GITHUB_CLIENT_SECRET}
    redirect-uri: ${GITHUB_REDIRECT_URI}

# SpecFlux Frontend URL (for OAuth redirects)
specflux:
  frontend:
    url: https://staging.specflux.dev
